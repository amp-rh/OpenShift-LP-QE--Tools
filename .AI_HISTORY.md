# AI History Log
This file tracks important changes and decisions in this directory and its sub-directories (when sensible).

## 2026-08-26
### Three-tier crash detection fallback in collect-guest.ps1
- Extended the guest collector to detect crashes that bypass the traditional BugCheck mechanism: LiveKernelEvent (Application/1001) and dirty-shutdown-only (System/6008).
- Adds `crash.detected` and `crash.crashType` fields to the output schema. Collects dumps from LiveKernelReports in addition to Minidump/MEMORY.DMP.
- Motivated by chaos testing finding: Driver Verifier Low Resources Simulation crashed the VM but produced no System/1001 or crash dump.
### Add chaos testing infrastructure for organic BSOD fault injection
- Added 12 organic fault injection triggers (chaos-triggers.json) and a chaos sweep script (sweep-chaos.sh) to apps/bsod-detector/.
- Expands beyond the 19 synthetic KeBugCheckEx triggers to include NMI injection, memory balloon squeeze, storage/device hot-remove, Driver Verifier stress modes, block I/O throttle, and Hyper-V enlightenment permutation testing.
- Produces real BSODs through actual failure conditions, validating both the detector and virtio driver resilience under KVM.

## 2026-08-21
### Add bsod-detector application
- Windows BSOD diagnostic-capture tool contributed as a new application under apps/bsod-detector/.
- Captures post-mortem evidence (bug-check codes, crash dumps, event logs) for OpenShift Virtualization Windows VM testing.
- Container image definition added under image/container/bsod-detector/.

## 2026-06-11
### TestReport--JUnit.sh — address etirta's latest PR #16 review comments
- Comment cleanup: removed "JUNIT_FILE is always in valid, parseable XML state after each call." (unnecessary info per reviewer).
- Usage line: corrected `[-f FAILURE_MSG | -e ERROR_MSG]` → `[-f MSG | -e MSG]` to match the Args section below it.
- TS_NAME comment: removed "Created automatically on first reference." (inaccurate — suite can be created on any call).
- `typeset __pyRC=0` → `typeset -i __pyRC=0` for type safety per Best Practices.
- Python: removed unused `import xml.etree.ElementTree as ET`.
- Python: aligned variable names with bash — `reportName` → `rptName`, `tcResult` → `tcRes`.
- Python: changed `except (FileNotFoundError, ET.ParseError)` → `except OSError` with `os.path.exists()` re-raise guard. Reason: junitparser uses lxml when cached, and lxml raises `OSError` (not `FileNotFoundError`) for missing files. `except OSError` catches both; the re-raise ensures any other OS error (e.g. permission denied) still propagates. Parse errors from lxml arrive as `XMLSyntaxError` (not `OSError`) and propagate naturally — satisfying etirta's "error out on anything except file-not-found" requirement.
- Python: moved `if tcRes == "failure" / elif tcRes == "error"` into the `else` clause of the `try` block — correct Python idiom for post-try-success logic.
- Python: removed redundant `suite.update_statistics()` — `suite.add_testcase()` already updates it, and `report.update_statistics()` cascades down.

## 2026-06-10
### TestReport--JUnit.sh — adopt remaining arg-parsing changes from etirta's suggestion
- Renamed `reportName` → `rptName` to match etirta's variable name.
- Changed `rptName` and `tsName` from `:?` to `?` (allows empty, rejects only unset) per etirta's suggestion.
- Updated all `:?` error messages to match etirta's exact wording ("as non-empty string.", "as numeric.", trailing periods).
- Replaced `[[ ... ]] || { ... ; return 1; }` TC_EXEC_TIME guard with bare `[[ ... ]]`, relying on `set -e` per etirta's style.
- Updated `uv run` arg from `"${reportName}"` to `"${rptName}"`.
- All 11 tests pass.
### TestReport--JUnit.sh — adopt etirta's ;;& fall-through case pattern
- Replaced separate `tcResult` variable + post-case `tcMsg` validation with etirta's `;;& ` fall-through pattern.
- `(-f)` and `(-e)` branches set `tcRes` in-place and fall through via `;;&` to `(-e|-f)` which validates `[ -n "${tcMsg}" ]`.
- Removed `typeset tcResult=""` and the explicit `[[ -z "${tcResult}" ... ]]` guard block.
- Updated `uv run` argument from `"${tcResult}"` to `"${tcRes}"` (same variable, now transformed in-place).
- All 11 tests pass.
### TestReport--JUnit.sh — address etirta's second round of PR #16 review comments
- Combined `typeset __shOpt` back to single line to match EnsureReqs.sh and Vault--BitWarden.sh convention (no linter enforced).
- Replaced all `echo "..." >&2` error messages with `: "..."` no-op pattern per best practices (avoids clutter in xtrace log).
- Restructured arg parsing: all positional args parsed first (`tcRes`, `tcMsg` via `${1:-}`), then validated in a separate block — per best practices "Parse all Function Arguments first, then sanitize them".
- Added extra-arg guard `(($#)) && { : "Unexpected extra arguments"; return 1; }` to reject invalid invocations (e.g. passing both -f and -e).
- Added `tcMsg` non-empty validation when `tcRes` is set — addresses "Some parameters may be set to empty even though required".
- All 11 tests pass.
### TestReport--JUnit.sh — address etirta's unresolved PR #16 review comments
### TestReport--JUnit.sh — address etirta's unresolved PR #16 review comments
- Removed top-level file comment block (docs belong inside the function's `###` block).
- Usage comment inside function now uses generic URL placeholder, not the hardcoded GitHub URL.
- Signature extended: added `REPORT_NAME` as 2nd positional arg; renamed `OUT_FILE`→`JUNIT_FILE`, `SECONDS`→`TC_EXEC_TIME`.
- Added REPORT_NAME mismatch validation in Python: `sys.exit(1)` if file's root node name differs from passed `REPORT_NAME`.
- Merged `-f` and `-e` into a single `case` branch with `if/else` to eliminate the duplicate mutual-exclusivity check.
- Python `len(sys.argv)` guard updated to 8 (one extra arg for `REPORT_NAME`); error message changed from "Internal error" to a proper `Usage:` string.
- All Python identifiers renamed to camelCase per repo convention (`junitFile`, `reportName`, `tsName`, `tcName`, `tcExecTime`, `tcResult`, `tcMsg`, `tmpOut`).
- Exit-code propagation fix: replaced `true` at end of function with `typeset __pyRC=0; uv run ... <<'PYEOF' || __pyRC=$?; return "${__pyRC}"`. Root cause: bash ignores `set -e` inside a function when that function was called in a context where `-e` was already off (e.g., `if ( fn )` or plain subshell with no parent `-e`); explicit `|| __pyRC=$?` captures the failure regardless.
- Test 11 added to `test_TestReport--JUnit.sh` to verify REPORT_NAME mismatch is rejected.
- All 11 tests pass; shellcheck --severity=style clean on both files.

## 2026-06-09
### TestReport--JUnit.sh — pre-commit review fixes (PR #16)
- Renamed Python variable `xml` → `report` to avoid shadowing the `xml` stdlib module (imported as `ET`). Not a runtime bug but a code smell Python linters flag.
- Replaced TOCTOU `if os.path.exists(tmp_out): os.unlink(tmp_out)` with `try/except FileNotFoundError` in the atomic-write `finally` block. Eliminates the check-then-act race; more Pythonic.
- All 10 tests pass, shellcheck --severity=style clean on both library and test script.
### TestReport--JUnit.sh — CodeRabbit review fixes (PR #16)
- Split combined `typeset __shOpt="$(cmd)"` into two lines to eliminate SC2155 shellcheck warning.
- Narrowed Python exception catch from bare `except Exception` to `except (FileNotFoundError, ET.ParseError)` so programming errors are not silently swallowed.
- Added `try/except ValueError` guard around `float(tc_time)` to make the Python block self-contained.
- Applied atomic write: `xml.write()` now writes to a `tempfile.mkstemp` temp file and uses `os.replace()` to swap it in atomically, preventing partial XML on mid-write interruption.
- Skipped the CodeRabbit `flock` locking suggestion — not applicable because `AddTC` is designed for sequential calls from a single step script, not concurrent processes.
- All 10 tests pass.

## 2026-04-16
### Agentic AI Mandate Updates
- Updated `.AI_INIT.md` and `.AI_README.md` to latest Best Practices template (fetch prompt, self-update steps, Scope, Extra AI Agent instruction files,
  General Conduct, hard sync rule for `.AI_INIT.md`).
- Added `AGENTS.md` and `GEMINI.md` symbolic links to `.AI_INIT.md` per corporate mandate.
- Added `CONTRIBUTING.md` with commit message conventions, PR workflow, and AI agent reference.
- Added `.coderabbit.yaml` for CodeRabbit AI code review configuration per corporate mandate.

## 2026-01-29
### Initial Repository Structure
- Created repository directory structure following modular design.
- Established `apps/` for application code, `image/container/` for container builds, `libs/` for reusable libraries (internal and external consumers), and
  `tools/` for maintenance scripts.
- Configured unified AI agent instruction system with `.AI_INIT.md` and symlinks for Claude Code, Continue.dev, and Cursor IDE.
- Directory naming convention established: `src/` (3 chars), `test/` (4 chars, optional), `docs/` (4 chars, optional).
